# conformance-report.md — Gate 1 自己監査（経営統合システム モック）

対象: keiei-togo-mockup ／ 正本: **要件定義書 v1.1** ＋ spec/（screens.yaml等）
記載ルール: **ズレ・仮定・未検証・定規違反のみ**（合格項目は書かない。Gate 2 で人が読むのはここだけ）。
**目的の再確認：本システムは提案用HTMLカンプ。判定基準＝HTMLとして動くか。実バックエンド結線は目的外。**

## 実装バッチ①（2026-07-20）— 権限是正6件 ＋ 予算→評価→給与のUI/UX強化
- **権限エンジン（app.js）**：`window.KV`（role/set/dept/tanto）＋`applyPerm()`＋`data-see`（setで表示）＋`[data-owner-filter]/data-tanto`（担当分に絞る）＋`window.onPermReady`フック。ロール切替（localStorage kv4_role）で表示範囲が変わる。
- **権-1 勤怠**：全社集計カードを `data-see="admin,kanri"`（管理部・経営のみ）／個人打刻は本人・委託対象外の明記／部門長はnav未収載。
- **権-3/4/5 顧客・案件・営業**：companies/deals/projects を担当分フィルタ（bucho=幸田／ippan=新谷／asst=新垣）。admin・kanriは全件。permBanner表示。
- **権-6/7 経営サマリー・予算**：nav sets に ippan/asst 追加（一般に開放）。詳細タイルは `data-see="admin,kanri,bucho"`＝一般は粗利・営業利益まで。
- **給与2枚看板（J-02）**：結果（今月給与＋次回賞与見込み・金額主役）／行動（何を頑張れば上がるか＝評価指標直結・role別）。根拠・契約類型・計算式はモーダル（金額の内訳／計算ルール）。「全体管理」タブは `data-see="admin,kanri"`（権-2）。
- **経営導線（J-03）**：予算(ST-01)→評価→給与(J-02)の繋がりバンドを `data-see="admin,kanri,bucho"`（従業員主画面には出さない）。評価member に「上げる一手」を前面（role別）。
- **未検証（重要）**：**本セッションはプレビューがタブ再読込しないため、ロール切替の"ライブ描画"（一般で予算の詳細が消える／給与2看板がrole別に出る／会社が担当分に絞られる）の実機キャプチャは未取得**。権限エンジンは budget で検証（KV populated・applyPerm有・admin=3タイル可視・ippanなら0＝ロジックと属性は正）。node構文・リンク・v52は合格。→ push後Vercelでロール切替の実挙動を要確認（Gate 2）。

- v0.1〜v0.5 は Gate 2 裁定済み
- **v0.6: 2026-07-20** — 提案用カンプの仕上げ（5点）。P-01提案品質化／経費追加→粗利がその場で動く（HTML/JS）／E-04端正化／評価・給与の"なぜ"面取り／死んでいる箇所の解消
- **合格基準（Gate 2 触って確認）**：①顧客ページが"提案の顔" ②経費追加→粗利が動く ③E-04端正 ④評価・給与の"なぜ"が一読で分かる ⑤死んでいる箇所がない
- **実演済み（HTML/JSで動く）**：経費追加→粗利/営業利益 再計算（E-04）／会社ステータス・担当フィルタ（E-01）／種別セグメント（E-03）／操作ログ追記（R-01）／ステータス・役務の遷移UI

---

## 1. ズレ（仕様と実装の差・Gate 2 判断が必要）

| # | 箇所 | 内容 |
|---|------|------|
| Z-16 | 売上経費の集計接続 | E-04 で案件別 売上・原価・販管費・粗利・営業利益を明細から算出（固定率廃止・実測 粗利40.5万=売上67.2−原価26.7／営業利益35.5万=粗利−販管費5.0）。ただし**経営サマリー・予実への実集計は注記接続まで**（budget 部門別に「E-04から積み上げ」明記）。全社P&Lの数値はサンプルで、案件明細との自動合算はモック未実装（U-03） |
| Z-17 | 操作ログの保存先 | C1 操作ログは localStorage(kv4_log)＋初回シード。ブラウザ/ロール跨ぎでは共有されない（モック）。実運用は HubSpot 監査＋自社ログ。日報の集約は当該ブラウザのログに対して決定論的 |
| Z-18 | 日報の「AI要約1行」 | C2 の要約1行は現状**静的サンプル**（検査対象と明記）。実生成（操作ログ→要約）は後フェーズ。事実の列挙部分は操作ログから決定論的に描画（動作） |
| Z-19 | config/rules.json の参照 | Track B の値は `config/rules.json` に集約したが、各画面（J-03/J-02/K-01/E-03）は現状**値を表示で反映（手書き一致）**。ランタイムfetchでの実参照は後フェーズ（A-09器まで）。数値の二重管理リスクは残る（正本はconfig・画面は追従の約束） |

**v0.6 再確認：Z-16（全社P&L合算）・Z-18（日報AI実生成）・Z-19（config実fetch）は「実バックエンド結線」であり本カンプの目的外。提案では「こう繋がります」を見せるまで（画面・経路・注記）で合格基準を満たす＝欠陥ではなく意図的スコープ。実結線は後フェーズ。**

（v0.1〜v0.4.1 の Z-01〜15 はクローズ／裁定済み。v0.6でZ-13死んでいる箇所＝companies統計チップ/担当を機能化・偽ページネーション撤去で解消）

## 2. 仮定（assumption-ledger）

| A-xx | v0.5での扱い |
|------|-------------|
| A-13 | **確定**：顧客契約＝受給が契約金額と同額（下限）／自社目標＝×2（到達目標）。E-04親子注記・E-05 BPO・rules・config に反映 |
| Z-14 | **確定**：成約＝契約書締結が当月（当月締め・任意期間セレクタ不要） |
| A-14 | **据え置き（仮）**：補助金子案件の器＝アップセル/AKC（ステージ=consult-status）。E-02/E-04/E-05に親子1:N表現・実wiringは後フェーズ |
| A-09 | Track B の器（config/rules.json）を用意するところまで（設定値化・ランタイム反映は後フェーズ） |
| A-10 | 賞与の変動ピッチ・給与テーブルは未定＝評価は褒賞原資の配分まで・給与は根拠表示まで（金額変換式なし） |
| A-04/A-05 | A-05確定運用：会社単位で発行・紐付け（1社1URL・LIFF起動パラメータ=会社）。A-04（トークン方式の詳細）は据え置き |

## 3. 未検証（バンドル不足により検証不能）

| # | 内容 |
|---|------|
| U-01 | 対応表 P-xx/W-xx・acceptance.feature 未同梱。ステータス/役務更新・成果報酬起票・子案件作成・ポータル発行・連携追加・ルール保存の書き込み仕様は unverified。モックは楽観更新なし・失敗分岐・操作ログで表現 |
| U-02 | 上位文書（引き継ぎ書第6章・要件定義書v1.0）との突合は未実施 |
| U-03 | 案件別売上経費→経営サマリー/予実の実集計、config/rules.json のランタイム参照、日報のAI実生成、LINE実配信は後フェーズ（画面・経路・「通知済み」状態まで実装） |

## 4. 定規違反（Q1〜Q9 自己監査。v0.5の新規・変更画面）

| Q | 箇所 | 内容 |
|---|------|------|
| Q4 | 全般 | card/tile 角丸併存（継続の軽微項目） |
| Q1/Q5 | R-01 通知フィード | 日報カードが均質（重要度の強弱が弱め）。停滞など要フォローの視覚強調は今後の磨き余地 |
| （参考） | ST-07/E-04/J-03/J-02/R-01/portal | Q2（根拠を近接配置）／Q5（点数化・給与根拠・操作ログを表・バッジで図化）／Q8（全値サンプル・生HEXゼロ）／Q9（バッジ⇄要素1対1・ホーム接続系統数=ST-07と一致）は違反なし |

## 5. 追加提案（実装していない・判断待ち）

- Z-16/U-03：案件明細→全社P&Lの自動合算（限界利益式の実計算）
- Z-19：config/rules.json のランタイムfetch参照（値の一元化を実効化）
- Z-18：日報AI要約の実生成＋検査（生成物レビュー）／LINEグループ実配信の結線
- A-09：会社独自ルールのドキュメント投入→AI構造化→承認→config反映の導出経路

## 6. 進捗列の更新

spec/screens.yaml 全20画面（R-01追加）の progress を更新: **kanpu=v**（R-01記録・通知を含め全画面到達可・孤立なし）。mapping・impl・accepted は "-"。
新規正本：Track B＝config/rules.json（ST-02が編集）。domain/ は anken-status（獲得・+キャンセル）＋consult-status（役務）。
