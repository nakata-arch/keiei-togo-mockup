# conformance-report.md — Gate 1 自己監査（経営統合システム モック）

対象: keiei-togo-mockup ／ 正本: keiei-togo-spec-bundle **v0.2**（repo内 `spec/`）
記載ルール: **ズレ・仮定・未検証・定規違反のみ**（合格項目は書かない。Gate 2 で人が読むのはここだけ）。

- v0.1 Gate 1: 2026-07-20（構造・H-01・E-02整列・E-03タブ・Q8）→ Gate 2 裁定済み
- **v0.2 差分適用: 2026-07-20**（D-1 E-04新設・遷移張替／D-2 spec移設／D-3 A-07確認）— 本レポートは v0.2 で更新

---

## 1. ズレ（仕様と実装の差・Gate 2 判断が必要）

| # | 箇所 | 内容 |
|---|------|------|
| Z-05 | D-3 / rules.html | **A-07確定（単体ルール画面は作らない）と残置ファイルの食い違い。** 追い指示 D-3 は「挙動変更なし・確認のみ」のため、今回は `rules.html`（v4.2で中田さんの明示依頼により作成した共通認識ルール画面）を**削除していない**。しきい値自体は使用箇所にインライン表示済み（K-01=50万円しきい値、exec/budget/cashflow/evaluation=合格ライン等）だが、それらから `rules.html` への参照リンクが残る。**削除するか／閲覧用として残すか**を Gate 2 で確定されたい（削除する場合は各画面の `→ルール` リンクをインライン注記へ置換する軽微改修が必要） |
| Z-06 | E-04 Deal ID | E-04 に表示する主キー「HubSpot Deal ID」はサンプル値（21894-0074）。IS-2026-NNNN（社内採番）と HubSpot Deal ID の対応・表示方針（どちらを主表示にするか）は未定。CLAUDE.md §0 は主キー=HubSpot Deal ID だが、画面上はIS番号が定着している |

（v0.1 の Z-01〜Z-04 は Gate 2 で解決／裁定済み：Z-01→E-04新設、Z-02/A-07→インライン確定、Z-03→ロール別内訳で運用、Z-04/A-08→バンドル外維持で確定）

## 2. 仮定（assumption-ledger に準拠して実装。A-01〜A-06 は中田確認待ち＝仮のまま）

| A-xx | 実装での扱い |
|------|-------------|
| A-01 | ナビを「経理処理（K-01/02）」「経営管理（M-01/02/03）」に分離。M-02 から編集 UI を撤去し ST-01 へ誘導 |
| A-02 | 評価・賞与（J-03）を「人事」グループに配置 |
| A-03 | 12段階ステータス未確定 → **E-03・E-04とも遷移ロジック未実装・表示のみ**。「（仮）」明記。domain/anken-status.state.mmd は作成保留のまま |
| A-04 | ST-07 の LIFF 設定は保存不可の下書き（パラメータ方式セレクタ disabled）。P-01 は起動パラメータ未実装の静的表示 |
| A-05 | P-01／E-04 に「いまお願いしていること」「次回予定」を表示（両者は同一データの顧客側／社内側） |
| A-06 | 50万円しきい値は K-01 の文言・ST-02 の編集項目として表示（値の保存はモックのため不可） |
| A-07 | **確定（Gate 2）**：ルール閲覧の単体画面は作らない・使用箇所にインライン・編集は ST-02。→ 実装はインライン済みだが rules.html 残置（Z-05） |
| A-08 | **確定（Gate 2）**：login / index / styleguide はバンドル外に維持 |

## 3. 未検証（バンドル不足により検証不能）

| # | 内容 |
|---|------|
| U-01 | **screens/ 配下（カンプ＋対応表 P-xx/W-xx）と write:true 画面の acceptance.feature がバンドル v0.2 にも未同梱**。K-01/K-02/ST-01〜07 の feature 判定は実施不能＝全件 unverified。モックでは失敗・差戻し分岐を UI 文言（差戻し理由必須・保存失敗時は反映しない・接続テスト成功/失敗の両表示・楽観更新なし）で表現済み |
| U-02 | 上位文書（引き継ぎ書第6章・要件定義書 v1.0）との突合は本改修では未実施（バンドルが画面実装レベルの正本のため） |

## 4. 定規違反（Q1〜Q9 自己監査。E-04 を含む）

| Q | 箇所 | 内容 |
|---|------|------|
| Q4 | E-04 ほか | bento 内で card（角丸11px）と tile（角丸14px）が併存。機能影響なし。角丸トークンの統一余地（v0.1から継続の軽微項目） |
| （参考） | E-04 | Q1（受給予定額248万を最大級・補足は淡色）／Q2（サマリー・ステータス・タスク・請求を別カードに近接集約）／Q3（12colグリッド整列）／Q5（ステータス=ステッパー、タスク=チェック、請求=バッジで図化）／Q6（左上に金額→ステータス→詳細）／Q8（全値サンプル明記・生HEXゼロ）は自己監査で違反なし |

## 5. 追加提案（実装していない。判断待ち）

- Z-05：rules.html を削除しインライン注記へ寄せる軽微改修（A-07を実装まで貫徹する案）
- E-04 のステータス正式名称が確定（A-03）したら、ステッパーを domain/anken-status.state.mmd 由来の遷移可否に接続（現在は表示のみ）

## 6. 進捗列の更新

spec/screens.yaml 全18画面（E-04追加）の progress を更新済み: **kanpu=v**（E-04含め全画面がモックとして存在・到達可能・孤立なし）／mapping・impl・accepted は "-"（対応表未同梱・本実装未着手のため）。
